let location = 'BBan';
let age = 18;
let boyos =18;

// variable name should be something meaningful. it will indicate whate it contains

let togodutogudutogutugudodu = 97;
let avc = 45;
let info = 'man majar';
let brb = 'Jasmin';

// do not use shorthand.
//its oky if the name is longer


// can not use a keyword (reserved word)
const let =26;
console.log(let);

// const false = "False";
 
//3. has to be in one word. can not contain any spage or gap
const her phone number = 44437353443;
const her_phone_number = 44437353443; //snake case
const herPhoneNumber = 44437353443;   //camel case. (usually this one we will use)
const HerPhoneNumber = 44437353443; // pascal case

// 4. name cannot start with a number. but number could be used anywhere other than the first letter

const 3Person = 3;
const person3 = 6;
const p3erson = 6;

// 5. other than letter name can start with either _ , $. cannot use any other special 
Character. *&#@ 

// 6. case sensive

let address= 'katabon';
let address= 'bandorbon';
let address= 'shalbon' ;

